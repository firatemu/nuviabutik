from django.urls import path
from . import views

app_name = 'downloads'

# Print Agent artık local kullanıldığı için URL'ler kaldırıldı
urlpatterns = [
    # Buraya ileride başka download linkleri eklenebilir
]